---
name: spotlight-reveal-photo
description: 把一张照片做成「鼠标/手指聚光灯揭示」效果的单文件网页。平时只显示去掉主体的空场景，指针移到时一个光圈跟随移动，靠近主体时光圈自动放大把整个人物（或物体）从画面里照出来。当用户想要这种「擦除式聚光灯」「鼠标扫过显露出照片里的人」「空场景中用灯光揭示人物」「cursor spotlight reveal photo」「hidden person flashlight web」「交互式照片揭示页」效果时使用。典型说法：「做一个鼠标移动显现照片里的人的网页」「用这张照片做个聚光灯揭示效果」「空背景鼠标照出人物」。
---

# Spotlight Reveal Photo（聚光灯揭示照片）

把一张照片做成纯视觉的单文件网页：底层是去掉主体的空场景，顶层是原图，顶层被一个跟随指针的圆形蒙版遮住——只有光圈内才看得到原图里的主体。

## 工作流程（按顺序执行）

### 1. 先向用户确认素材
不要假设图片。先问清并拿到：
- 用户的照片（附件或 URL）
- 照片里要被「揭示」的主体是谁/是什么（一个人？一个物？），以及它大致在画面的哪个位置（左上/中间/右下……）
- 是否需要在两边/上下留纯色边，背景色偏好（默认取照片天空/背景的主色）

### 2. 生成「空场景」图
用图片编辑能力，基于用户原图生成一张**去掉主体、背景自然补全**的版本：
- 指令要点：移除<主体>，身后/周围背景按周围环境自然补全，保持构图、光线、色调、分辨率与原图一致
- 这张作为底层；用户原图作为顶层

两张图必须**同一场景、同一构图、只差主体本身**，否则光圈边缘会错位。

### 3. 标定主体坐标（关键，决定光圈放大位置对不对）
在原图的**像素坐标系**里确定三个数：
- `IMG_W` / `IMG_H`：原图实际宽高（像素）
- `PERSON.x` / `PERSON.y`：主体中心（比如人脸中心）在原图里的像素坐标
- `PERSON.extent`：要把整个人照出来需要的半径——从主体中心到最远部位的距离（像素）

估算方法：主体中心 = 脸/物体中心；extent ≈ 覆盖整个人所需半径（约等于人身高的 0.55~0.6 倍，先估一个，预览不对再调）。

### 4. 套用模板产出 index.html
读取 `assets/template.html`，把下列占位符全部替换成真实值，另存为任务目录下的 `index.html`：

| 占位符 | 含义 |
| --- | --- |
| `{{TITLE}}` | 网页 `<title>` |
| `{{BG_COLOR}}` | 页面底色（取照片背景主色，使留白与画面融合，如 `#63B3E8`） |
| `{{BASE_IMG_URL}}` | 空场景图 URL（底层） |
| `{{REVEAL_IMG_URL}}` | 用户原图 URL（顶层，带主体） |
| `{{IMG_W}}` `{{IMG_H}}` | 原图像素宽高 |
| `{{PERSON_X}}` `{{PERSON_Y}}` `{{PERSON_EXTENT}}` | 第 3 步标定的三个数 |

### 5. 自检
- 本地用浏览器打开验证：空场景正常显示；鼠标/手指移动时光圈跟随；移到主体上光圈平滑放大、整个人被照出来
- 重点核对：光圈放大后是否正好罩住主体（不对就微调 `PERSON.x/y/extent`）
- 确认图片**没有被裁切或平铺**

## 关键实现要点（为什么要这么写）

- **两层绝对定位叠放**：底层空场景 z-index 1，顶层原图 z-index 2；顶层加 `mask-image`（同时写 `-webkit-mask-image` 兼容）。
- **`background-size: contain` + `background-repeat: no-repeat`**：竖图要完整显示。**漏写 no-repeat 会导致图片在留白处横向平铺成多份**（最常见 bug）。
- **坐标换算必须与 contain 居中一致**：`s = min(w/IMG_W, h/IMG_H)`，居中偏移 `offX/offY = (容器尺寸 - 原图渲染尺寸)/2`。JS 里的换算和 CSS 的 `center center` 必须对应，否则光圈放大位置会偏。
- **半径平滑过渡**：用 `curR += (targetR - curR) * 0.2` 做 lerp，光圈变大/变小才不生硬。
- **触屏**：同时监听 `mousemove` 和 `touchmove`（passive），手机上手指滑动也能用。
- **纯静态**：无框架、无构建工具，全部内联在一个 html 里。
